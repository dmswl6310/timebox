# Timebox 작업 가이드

## 기본 원칙

- 사용자 화면과 문서는 한국어를 기본으로 작성한다.
- 파일은 UTF-8로 유지하며 깨진 문자열이나 NUL 문자를 커밋하지 않는다.
- 기존 사용자 변경을 덮어쓰지 않는다.
- 소스 수정은 작은 기능 단위로 나누고 관련 없는 리팩터링을 섞지 않는다.
- 커밋 메시지는 한글로 작성한다.

## 아키텍처 경계

- `app/page.tsx`와 공개 페이지는 가능한 한 React Server Component로 유지한다.
- 브라우저 상호작용만 `features/planner/timebox-dashboard.tsx`에 둔다.
- Supabase 읽기는 `server-data.ts` 또는 `records-data.ts`, 쓰기는 `persistence.ts`에 둔다.
- 플래너 도메인 규칙과 낙관적 상태 변경은 `store.tsx`에서 관리한다.
- Client Component를 추가하기 전에 Server Component로 해결할 수 있는지 먼저 확인한다.

## 도메인 불변식

- 서울 시간대(`Asia/Seoul`)와 `YYYY-MM-DD` 계획 날짜를 명시적으로 사용한다.
- 사용자는 전날에도 내일 계획을 작성하고 확정할 수 있다. 미래 계획의 `closed` 전환은 해당 날짜가 된 뒤에만 허용한다.
- 하나의 작업은 같은 날짜의 활성 일정 블록 하나에만 연결한다.
- 태그 이름은 공백을 정규화해 최대 30자로 저장하고, 사용자 정의 태그에도 결정적인 색상을 적용한다.
- 주간 목표는 서울 기준 월요일 시작일에 귀속하며 30분 단위의 목표 시간으로 저장한다.
- 일정 블록과 연결 작업의 예상 시간은 서로 어긋나지 않게 유지한다.
- 완료 블록은 수동 실제 시간이 없으면 계획 시간을 기본 실제 시간으로 저장한다.
- 같은 화면에서 발생한 Supabase 쓰기는 사용자 조작 순서를 보존한다.
- 일정은 15분 단위이며 05:00부터 24:00 범위를 벗어나지 않는다.
- 현재 단일 레인 시간표에서는 블록 겹침을 허용하지 않는다.
- 삭제는 `time_blocks.status = 'cancelled'`로 처리한다.
- 계획 확정 전 편집은 기준 계획에 포함한다. 확정 후 일정 추가·이동·크기 변경·삭제에는 이유가 필요하다. 중간 편집은 이력으로 쌓지 않고, 일과 완료 시 확정본과 최종본의 차이와 각 변경 유형의 마지막 이유만 저장한다.
- 일과를 완료해 `closed`가 된 계획은 더 이상 일정·완료·실제 시간을 수정하지 않는다.
- 완료된 작업은 브레인덤프에서 숨겨도 과거 일정과 통계 데이터는 보존한다.

## Supabase

- 모든 사용자 소유 테이블은 RLS와 `user_id` 조건을 유지한다.
- 운영에 적용된 마이그레이션은 수정하지 않는다. 변경은 다음 번호의 새 SQL 파일로 추가한다.
- DDL은 `supabase/migrations`에 기록한 뒤 적용한다.
- 공개 공유 기능에는 원문 토큰을 저장하지 않는다.
- 공유 링크 목록에는 토큰 원문을 반환하지 않으며 취소는 소유자와 활성 링크에만 허용한다.
- 쿼리는 가능한 한 현재 계획 또는 필요한 ID 목록으로 범위를 제한한다. 장기 기록 조회는 고정 `limit`으로 잘라내지 않고 500건씩 페이지 조회하며, `in` 조건의 UUID는 100개 단위로 나눈다.

## UI와 성능

- 모바일에서 터치 가능한 영역은 최소 40px 안팎을 확보한다.
- 드래그 중에는 레이아웃 속성보다 transform 기반 움직임을 선호한다.
- 긴 목록이나 통계 조회는 플래너의 편집 상태와 분리한다.
- 데모와 로그인 사용자의 동작 차이를 명확히 표시한다.
- 비활성 버튼에는 이유를 알 수 있는 레이블이나 안내를 제공한다.

## 완료 조건

변경 후 아래 명령을 모두 실행한다.

```bash
npm run typecheck
npm run lint
npm run build
```

사용자 흐름을 바꾼 경우 데스크톱과 모바일 폭에서 직접 확인한다. DB 변경이 있으면 RLS와 실제 원격 스키마도 확인한다.
